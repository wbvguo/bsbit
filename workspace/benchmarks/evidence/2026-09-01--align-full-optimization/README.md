# `bsbit align` full optimization — 2026-09-01

This evidence belongs to branch `codex/align-full-optimization`, based on the
completed exploratory branch at commit `a8f4436`.

The five-million-fragment GRCh38 fixture, large indexes, release binaries,
BAMs, and `perf.data` files remain outside Git under
`/tmp/bsbit-align-full-optimization-20260901`. This directory retains selected
raw timings, internal pipeline metrics, commands, interpretation, and exact
output digests.

Files:

- `report.md`: conclusions, tradeoffs, and remaining ceiling.
- `results.tsv`: selected whole-program raw timing rows.
- `paired-metrics.tsv`: selected paired pipeline telemetry.
- `commands.md`: build, run, profile, and verification commands.

The earlier discovery evidence and its reusable immutable-run harness are in
`../2026-09-01--align-performance-experiments/`.
